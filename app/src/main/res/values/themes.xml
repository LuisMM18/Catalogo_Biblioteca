<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Catalogo_Biblioteca" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores de marca basados en el diseño -->
        <item name="colorPrimary">@color/coral_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/bg_warm</item>
        <item name="android:statusBarColor">@color/bg_warm</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Catalogo_Biblioteca" parent="Base.Theme.Catalogo_Biblioteca" />
</resources>